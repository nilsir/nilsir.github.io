const { defaultTheme } = require("vuepress");

module.exports = {
  lang: 'zh-CN',
  title: 'Nilsir\'s Blog',
  description: 'nilsir\'s blog on github pages',
  theme: defaultTheme({
    darkMode: true,
  })
}
